
export const OpenMenuOrderMobile = () => {
    const buttonFilterOder = document.getElementById('#buttonFilterOrder')
    const menuOrderMobile = document.getElementsByClassName('.menuOrderMobileStyle')

    // if(menuOrderMobile)
    console.log(buttonFilterOder)
    buttonFilterOder.addEventListener('click', () => {
        console.log("oK")
    })
}
OpenMenuOrderMobile()